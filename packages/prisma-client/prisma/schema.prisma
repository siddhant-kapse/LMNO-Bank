// packages/prisma-client/prisma/schema.prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model User {
  id        String   @id @default(auto()) @map("_id") @db.ObjectId
  username  String   @unique
  password  String
  createdAt DateTime @default(now())
  customers Customer[] @relation("UserToCustomer")
}

model Customer {
  id           String   @id @default(auto()) @map("_id") @db.ObjectId
  customerId   Int      @unique
  username     String   @unique
  user         User     @relation("UserToCustomer",fields: [username], references: [username])
  firstName    String
  lastName     String
  phoneNumber  String
  address      String
  pan          String
  aadharNo     String
  accounts     Account[]
  transactions Transaction[]
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
  balance      Int @default(100)
}

model Account {
  id            String    @id @default(auto()) @map("_id") @db.ObjectId
  accountNumber String    @unique
  balance       Float     @default(10000.0)
  customer      Customer  @relation(fields: [customerId], references: [customerId])
  customerId    Int
  createdAt     DateTime  @default(now())
}

model Transaction {
  id              String   @id @default(auto()) @map("_id") @db.ObjectId
  transactionType String   // "DEPOSIT", "WITHDRAWAL", "TRANSFER"
  amount          Float
  description     String?
  customer        Customer @relation(fields: [customerId], references: [customerId])
  customerId      Int
  createdAt       DateTime @default(now())
}

model Counter {
  key   String @id @map("_id")
  value Int
}